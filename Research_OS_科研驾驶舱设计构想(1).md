# 科研进度与智能时间规划器：设计构想

## 1. 产品定位

这个系统不做复杂的"Research OS"，第一阶段只定位为：

> **一个以"今日安排"为主界面、以"本周课表"为辅助界面的科研进度管理器与智能时间规划器。**

它主要解决三个问题：

1.  我今天到底应该先做什么？
2.  多个任务同时存在时，怎样根据截止时间、预计耗时和重要性合理排序？
3.  我这一周、这半个月到底推进了哪些科研工作？

系统应尽量简单，避免为了管理科研而花大量时间维护管理系统。

------------------------------------------------------------------------

## 2. 当前主要科研任务

目前主要工作线包括：

-   **实验数据处理**

    -   Python + pandas
    -   公开实验数据集
    -   当前主要问题：数据噪声难以有效去除

-   **专业课程学习**

    -   当前学习到 核心章节
    -   下一步：基础模型、进阶模型、专题模型推导

-   **数据分析工具开发**

-   **数据处理流程监控工具**

    -   后续再逐步开发
    -   当前不是最高优先级

-   **每日论文阅读**

    -   高精度监测方法
    -   算法优化
    -   工程应用
    -   风险评估等

-   **临时任务**

    -   导师任务
    -   项目任务
    -   临时数据处理
    -   其他突然插入的工作

------------------------------------------------------------------------

## 3. 核心设计原则

### 3.1 首页只看今天

**主界面必须是"今日安排"，不能默认展示整周。**

原因：

-   每天打开系统时，只需要面对今天；
-   避免整周大量任务同时出现造成压力；
-   更容易进入执行状态；
-   本周安排作为辅助页面，需要时再查看。

### 3.2 本周安排采用"课程表"形式

副界面采用类似学校课程表的周视图：

-   横向：周一至周日
-   纵向：时间
-   每个任务根据预计持续时间占据对应时间块

主要用于：

-   周计划；
-   查看本周总体负载；
-   调整跨天任务；
-   查看哪些天还有空闲时间。

### 3.3 系统服务于科研，而不是增加管理负担

第一阶段不引入：

-   复杂 RAG；
-   向量数据库；
-   多 Agent 系统；
-   复杂事件溯源；
-   大型数据库；
-   复杂项目管理流程。

重点就是：

> **任务 → 时间安排 → 完成记录 → 科研进度。**

------------------------------------------------------------------------

## 4. 工作时间

默认可安排工作时间：

> **09:00 -- 20:30**

系统所有自动排程默认在这个时间范围内进行。

但不能把 09:00--20:30 全部塞满。

建议：

-   深度工作块：90--150 分钟；
-   任务间缓冲：10--20 分钟；
-   每日只自动占用约 75%--80% 的可用时间；
-   保留临时任务和 Debug 超时空间；
-   午间可以设置休息或低强度任务区间。

这样即使突然出现导师任务、项目任务或者 数据处理 Debug
超时，也不会导致整天计划完全崩溃。

------------------------------------------------------------------------

## 5. 主界面：今日安排

首页核心是当天时间轴。

示意：

``` text
┌──────────────────────────────────────┐
│  TODAY                     8月16日   │
│  可安排时间：09:00–20:30             │
└──────────────────────────────────────┘

🎯 Today's Big 3

1. 实验数据噪声排查
2. 课程专题模型推导
3. 数据分析工具开发


09:00 ─────────────────────────────

       专业课程学习
       专题模型推导
       预计 2 h

11:00 ─────────────────────────────

       缓冲 / 休息

11:20 ─────────────────────────────

       实验数据处理
       预计 2 h

13:20 ─────────────────────────────

       午间休息

14:30 ─────────────────────────────

       数据分析工具
       预计 1.5 h

16:00 ─────────────────────────────

       临时任务 / 论文 / Debug

...

20:30 ─────────────────────────────
```

首页顶部只需要显示：

-   Today's Big 3；
-   今天剩余可用时间；
-   当前正在进行的任务；
-   下一个任务；
-   "智能重新安排"按钮。

------------------------------------------------------------------------

## 6. 副界面：本周课表

本周页面采用课程表布局。

示意：

``` text
         周一        周二        周三        周四        周五

09:00   课程学习
        │
10:00   │
        │
11:00   ┘

14:00               数据处理
                    │
15:00               │
                    │
16:00               ┘

17:00                           数据分析
                                │
18:00                           ┘
```

本周页面主要负责：

-   查看整体工作负载；
-   调整任务到不同日期；
-   查看 Deadline；
-   查看未安排任务；
-   查看本周科研时间分布。

**默认进入系统时不显示这一页。**

------------------------------------------------------------------------

## 7. 任务数据

任务第一版只需要保存必要信息：

``` json
{
  "id": "task_001",
  "title": "实验数据噪声排查",
  "project": "Data Processing",
  "duration_minutes": 120,
  "deadline": "2026-08-17T18:00:00",
  "importance": 5,
  "status": "todo",
  "splittable": false
}
```

核心字段：

  字段         含义
  ------------ ------------------------
  title        任务名称
  project      所属科研项目
  duration     预计耗时
  deadline     截止时间
  importance   重要程度
  status       未开始 / 进行中 / 完成
  splittable   是否允许拆分

第一版不要增加太多字段。

------------------------------------------------------------------------

## 8. 固定事件与弹性任务

任务分为两类。

### Fixed：固定事件

例如：

-   上课；
-   组会；
-   导师会议；
-   项目会议；
-   已经确定时间的事情。

例如：

``` json
{
  "type": "fixed",
  "start": "14:00",
  "end": "15:30"
}
```

系统不能自动移动。

### Flexible：弹性科研任务

例如：

-   实验数据处理；
-   理论学习；
-   写代码；
-   阅读论文；
-   数据分析工具开发。

例如：

``` json
{
  "type": "flexible",
  "duration_minutes": 120
}
```

系统根据当天空闲时间自动安排。

------------------------------------------------------------------------

## 9. 智能任务推荐

系统最重要的智能功能不是聊天，而是：

> **告诉我接下来最应该做哪个任务，并自动找到合适的时间块。**

例如同时存在：

### 任务 A

``` text
实验数据噪声问题
预计：2 h
截止：今天 18:00
重要度：5
阻塞后续处理：是
```

### 任务 B

``` text
数据分析工具页面
预计：1.5 h
截止：今天 18:00
重要度：4
阻塞后续：否
```

系统可以推荐：

``` text
14:00–16:00  实验数据处理
16:15–17:45  数据分析工具
```

即使 B 更短，也不一定先做 B。

------------------------------------------------------------------------

## 10. 第一版任务排序逻辑

第一版不需要让 LLM 完全决定排序。

先采用规则算法：

``` text
Priority Score
=
截止紧迫度
+ 重要程度
+ 截止风险
+ 是否阻塞其他任务
+ 项目优先级
```

可以采用类似：

``` text
Score =
0.30 × Urgency
+ 0.25 × Importance
+ 0.20 × DeadlineRisk
+ 0.15 × Blocking
+ 0.10 × ProjectPriority
```

随后进行时间可行性检查：

``` text
任务排序
   ↓
检查 Deadline
   ↓
检查预计耗时
   ↓
检查今天剩余时间
   ↓
寻找连续空闲时间块
   ↓
自动排入今日时间轴
```

LLM / DeepSeek 后续可以负责：

-   理解自然语言输入的任务；
-   判断任务大致重要程度；
-   对推荐结果进行解释；
-   分析为什么任务总延期；
-   根据最近完成情况提出调整建议。

但最终排程最好仍然由确定性的规则完成。

------------------------------------------------------------------------

## 11. 深度工作任务不能被过度拆分

科研任务和普通 TODO 不一样。

例如：

> 课程专题模型推导：2 h

应该优先安排：

``` text
09:00–11:00
```

而不是：

``` text
09:00–09:30
11:30–12:00
15:00–15:30
19:00–19:30
```

因此任务可以设置：

``` json
{
  "splittable": false,
  "minimum_block_minutes": 90
}
```

对于：

-   理论推导；
-   写论文；
-   数据处理 Debug；
-   编程开发；

优先保证连续深度工作时间。

------------------------------------------------------------------------

## 12. 临时任务处理

系统必须允许随时加入任务。

例如：

``` text
+ 新任务
```

输入：

``` text
导师让我明天下午之前做一个结果图，大约需要 1 小时。
```

系统解析：

``` text
预计耗时：1 h
Deadline：明天下午
重要度：较高
类型：Flexible
```

然后重新计算任务安排。

原则：

> **新任务进入后，只调整必要的时间块，不重新打乱整个星期。**

------------------------------------------------------------------------

## 13. 科研项目进度

项目进度页面保持简单。

例如：

``` text
实验数据处理
████████░░  60%

当前：
数据噪声问题

下一步：
验证数据一致性 / 相关性


数据分析工具
██████░░░░  45%

当前：
XXX 模块开发


专业课程学习
█████░░░░░  40%

当前：
核心章节

下一步：
基础 / 进阶 / 专题模型
```

项目进度不要求非常精确。

核心是能够快速回答：

> **这个项目现在做到哪里了？**

------------------------------------------------------------------------

## 14. 每日记录

每天结束时只记录很少的信息：

``` json
{
  "date": "2026-08-16",
  "completed": [
    "完成专题模型推导",
    "完成实验数据噪声第一轮排查"
  ],
  "problems": [
    "数据校正后仍存在长波噪声"
  ],
  "next": [
    "检查数据一致性"
  ]
}
```

不要求写复杂日报。

每天花 2--5 分钟即可。

------------------------------------------------------------------------

## 15. 半月报

每月：

-   1--15 日；
-   16 日--月底；

系统读取：

-   每日完成任务；
-   项目进度；
-   Daily Log；
-   问题记录；
-   论文阅读记录。

交给云端 AI 自动生成半月报草稿。

因此不需要保存所有 AI 对话作为核心数据。

真正重要的是：

> **每天实际做了什么。**

------------------------------------------------------------------------

## 16. 数据结构

第一版保持极简：

``` text
data/
├── projects.json
├── tasks.json
├── schedule.json
├── daily_logs.json
└── settings.json
```

其中：

### projects.json

项目状态。

### tasks.json

所有科研任务。

### schedule.json

每天实际安排到哪个时间段。

### daily_logs.json

每天完成情况、问题和下一步。

### settings.json

例如：

``` json
{
  "work_start": "09:00",
  "work_end": "20:30",
  "schedule_fill_ratio": 0.78,
  "default_break_minutes": 15
}
```

------------------------------------------------------------------------

## 17. 页面结构

第一版只做 4 个页面。

### ① Today

**默认首页。**

显示：

-   Today's Big 3
-   今日时间轴
-   当前任务
-   下一任务
-   新增任务
-   智能重新安排

### ② Week

课程表形式。

显示：

-   周一至周日；
-   每天 09:00--20:30；
-   Fixed Event；
-   Flexible Task；
-   Deadline。

### ③ Tasks / Projects

显示：

-   待安排任务；
-   已完成任务；
-   项目进度；
-   新增任务。

### ④ Review

显示：

-   今日完成；
-   本周完成；
-   最近 15 天；
-   半月报生成。

底部导航可以设计为：

``` text
今日     本周     任务     进度
 ●
```

------------------------------------------------------------------------

## 18. 推荐技术栈

保持简单：

``` text
Frontend
React + TypeScript + Vite

UI
Tailwind CSS

Data
JSON

AI
Cloud LLM API

Deploy
GitHub Pages

Backup
本地 Windows / Private Git Repository
```

第一版暂时不需要：

``` text
PostgreSQL
Redis
Vector Database
RAG
FastAPI
Docker
复杂 Agent Framework
```

后续真的出现需求再增加。

------------------------------------------------------------------------

## 19. 第一版 MVP

第一版只实现：

``` text
Today
│
├── Today's Big 3
├── 09:00–20:30 时间轴
├── 新增任务
└── 智能安排

Week
│
└── 周课程表

Tasks
│
├── Duration
├── Deadline
├── Importance
└── Status

Progress
│
├── Project Progress
└── Daily Log
```

AI 第一版只需要解决：

> **"我接下来应该做哪个任务？"**

和：

> **"这些任务应该怎么塞进今天剩余的时间？"**

------------------------------------------------------------------------

## 20. 当前产品定义

最终可以把第一阶段产品定义压缩成一句话：

> **一个面向科研工作的智能时间规划器：以今日安排为主界面，以本周课程表为辅助，根据任务耗时、截止时间、重要性和项目优先级自动安排
> 09:00--20:30
> 的科研时间，并记录实际完成情况，为每周复盘和半月报提供依据。**

核心原则：

> **今天优先，整周辅助；任务少而明确；计划留有余量；科研进度比清空 TODO
> 更重要。**
