# Research OS 云同步（仅浏览器可公开配置）
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_REPLACE_ME

# 安全红线：
# 1. 不要在这里填写 sb_secret_*。
# 2. 不要填写 legacy service_role key。
# 3. AI API Key 继续由用户在 Research OS 设置页填写，只保存在本机，不放进 .env。
